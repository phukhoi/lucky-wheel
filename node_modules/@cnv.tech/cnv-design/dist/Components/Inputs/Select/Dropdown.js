"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const utils_1 = require("../../../utils");
const _name = 'select-dropdown';
const SelectDropdown = props => {
    let attrs = getAttrs(props);
    if (attrs) {
        let { options } = props;
        return react_1.default.createElement("div", Object.assign({}, attrs), options);
        // return <Portal>
        //     <div {...attrs}>
        //         {options}
        //     </div>
        // </Portal>
    }
    return null;
};
const getAttrs = (props) => {
    let { isDropdowning, indexOption = 3 } = props;
    indexOption < 0 && (indexOption = 3);
    if (isDropdowning) {
        // let rectTop = parentRect.y + parentRect.height + 5
        // let rectLeft = parentRect.x
        // let rectWidth = parentRect.width
        return {
            className: utils_1.utils.getClassNames(utils_1.utils.getName(_name), {
                'hide': !isDropdowning
            }),
            style: {
                zIndex: indexOption
            }
            // style: {
            //     top: rectTop,
            //     left: rectLeft,
            //     width: rectWidth
            // }
        };
    }
    return null;
};
exports.default = SelectDropdown;
//# sourceMappingURL=Dropdown.js.map