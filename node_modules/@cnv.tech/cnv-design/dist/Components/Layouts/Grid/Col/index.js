"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Col = void 0;
const react_1 = __importDefault(require("react"));
const Col = props => {
    let { children, className = '', size, xl = false, lg = false, md = false, sm = false, xs = false } = props, others = __rest(props, ["children", "className", "size", "xl", "lg", "md", "sm", "xs"]);
    className += getSizeClass('', size);
    className += getSizeClass('xl', xl);
    className += getSizeClass('lg', lg);
    className += getSizeClass('md', md);
    className += getSizeClass('sm', sm);
    className += getSizeClass('xs', xs);
    return react_1.default.createElement("div", Object.assign({ className: `col ${className}` }, others), children);
};
exports.Col = Col;
const getSizeClass = (name, size) => {
    let type = typeof size;
    if (type === 'boolean' && !size)
        return '';
    let n = name ? '-' + name : '';
    let s = (type === 'number' || type === 'string') ? '-' + size : '';
    return ` col${n}${s}`;
};
//# sourceMappingURL=index.js.map