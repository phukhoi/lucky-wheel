"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const utils_1 = require("../../../../utils");
require("./index.css");
const _name = 'box';
const _nameTitle = 'box-title';
const Box = props => {
    let { title, children } = props;
    let attrs = getAttrs(props);
    return react_1.default.createElement("section", Object.assign({}, attrs),
        title
            && react_1.default.createElement("div", { className: utils_1.utils.getClassNames(utils_1.utils.getName(_nameTitle), 'f-w-500 px-2') }, title),
        children);
};
const getAttrs = (props) => {
    let { className, style, children } = props;
    return {
        className: utils_1.utils.getClassNames(utils_1.utils.getName(_name), {
            'border': !checkIsBoxWrapper(children)
        }, className),
        style: style
    };
};
const checkIsBoxWrapper = (children) => {
    if (children) {
        if (Array.isArray(children))
            for (let i = 0; i < children.length; i++) {
                if (checkIsBoxWrapper(children[i]))
                    return true;
            }
        else if (children.type === react_1.default.Fragment) {
            if (checkIsBoxWrapper(children.props.children))
                return true;
        }
        else if (children.type === Box)
            return true;
    }
    return false;
};
exports.default = Box;
//# sourceMappingURL=index.js.map